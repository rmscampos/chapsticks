<%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <td>Brand</td>
      <td>Flavor</td>
      <td>Category</td>
      <td>Season</td>
    </tr>
  </thead>
<tbody>
  <% if (chapsticks.length > 0) { %>
   <tr>
      <% chapsticks.forEach(function(c) { %>  
        <td><%= c.brand %></td>
        <td><%= c.flavor %></td>
        <td><%= c.category %></td>
        <td><%= c.season %></td>
        <td><a href="/chapsticks/<%= c._id %>">REVIEWS</a></td>
        <td><a href="/chapsticks/<%= c._id %>">Edit</a></td>
        <form action="/chapsticks/<%= c._id %>?_method=DELETE" class="delete-form" method="POST">
          <td><button type="submit">X</button></td></form>
      </tr>
      <% }); %>
      <% } else {  %>
        <h3><%= "You've got nothing!" %></h3>
      <% } %>
    </body>
  </table>

<%- include('../partials/footer') %>